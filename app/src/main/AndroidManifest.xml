<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mervebozkurt.tariflercarpisiyor">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".UI.FoundRecipeListActivity"></activity>
        <activity android:name=".Profile.ShowProfile">

        </activity>
        <activity android:name=".Profile.UpdateUserProfile" />
        <activity android:name=".Profile.CreateProfileActivity" />
        <activity android:name=".UI.ShowRecipeByCategoryActivity" />
        <activity android:name=".UI.RecipeDetailActivity" />

        <activity android:name=".UI.UploadRecipeActivity" />
        <activity android:name=".Login.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Login.SignUpActivity" />
        <activity android:name=".UI.MainActivity" />
    </application>

</manifest>